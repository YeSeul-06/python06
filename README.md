# python 6번째 과제
### 강의 노트 7번 39~45
-------------------------------------------------------------------------------------------------

## 39번
1. 0부터 숫자 포함까지 홀수여야 하므로 *range* 안에 0부터 n+1이라 적음.
2. *if 조건문*을 사용하여 i를 2로 나눴을때 0이 아니면 출력하게 적음.
3. 사용자를 입력 받아야 함으로 *input*, 정수여야 하므로 *int*를 적어주고 print하기.

- 결과 : 5 입력
- 1
- 3
- 5
- None
------------------------------------------------------------------------------------------------------------------------------

## 40번
1. 39번과 같이 *range* 안에 0부터 n+1이라 적음.
2. 조건문에는 3의 배수여야 함.
3. 사용자를 입력 받아야 함으로 *input*, 정수여야 하므로 *int*를 적어주고 print하기.

- 결과 : 7 입력
- 0
- 3
- 6
- None
------------------------------------------------------------------------------------------------------------------------------

## 41번
1. 매개변수가 4개 여야 하므로 a, b, c, d를 *list*로 받기.
2. 최댓값과 최솟값을 리턴해야 하므로 *return*을 적기.
3. a, b, c, d에 각각 사용자로붜 입력 받아야 함으로 *input*, 정수여야 하므로 *int*를 적어주고 print하기.
4. 이때 *min*과 *max*에 a, b, c, d를 넣고 print 하기.

- 결과 : 6, 5, 1, 8을 각각 입력
- 최댓값: 8
- 최솟값: 1
------------------------------------------------------------------------------------------------------------------------------

## 42번
1. 0부터 숫자 포함까지 홀수여야 하므로 *range* 안에 0부터 n+1이라 적음.
2. *if 조건문*을 사용하여 i를 2로 나눴을때 0이 아니면 출력하게 적음.
3. 사용자를 입력 받아야 함으로 *input*, 정수여야 하므로 *int*를 적어주고 print하기.

- 결과 : 8 입력
- 1
- 3
- 5
- 7
- None
------------------------------------------------------------------------------------------------------------------------------

## 43번
1. 처음엔 0보다 큰 정수를 받아야 하므로 *result*엔 1이 들어가야 함.
2. 1부터 숫자 포함까지 포함되야 하므로 *range* 안에 1부터 n+1이라 적음.
3. 펙토리얼을 구하려면 i를 모두 곱해야 함.
4. 그리고 *reulst*로 반환하기.
5. 사용자를 입력 받아야 함으로 *input*, 정수여야 하므로 *int*를 적어주고 print하기.

- 결과 : 5 입력
- 120
------------------------------------------------------------------------------------------------------------------------------

## 44번
1. 2개(i,j)을 받아야 함으로 *total*은 0이 있어야 함.
2. i : 1부터 숫자 포함까지 포함되야 하므로 *range* 안에 1부터 n+1이라 적음.
3. 그 바로 아래에 j : 1부터 숫자 포함까지 포함되야 하므로 *range* 안에 1부터 n+1이라 적음.
4. *if 조건문*으로 x와 y의 곱이 30 이상이면 total이 둘 곱한 합과 같다고 써야 함.
5. 그리고 total을 반환하기.
6. 사용자를 입력 받아야 함으로 *input*, 정수여야 하므로 *int*, 2개를 받아야 하므로 *map*을 적어주고 print하기.

- 결과 : 5 6 입력
- 30
------------------------------------------------------------------------------------------------------------------------------

## 45번
1. 결과 값에 해당하는 result에는 아무것도 없어야 하므로 [], 또 total에도 0
2. *for문*이 n까지 돌때 total은 i에 들어있는 합들을 누적해서 출력해줘야 함.
3. result를 반환하기.
4. 리스트를 함수로 입력 받아야 하므로 *input*, 정수여야 하므로 *int*, 2개 이상을 받아야 하므로 *map*을 적어주고 print하기

- 결과 : 1 2 3 4 5 입력
- [1, 3, 6, 10, 15]

-------------------------------------------------------------------------------------------------
1년동안 파이썬 수업을 가르쳐 주셔서 감사합니다. 남은 2년 동안 계속 상담 찾아갈게용! 😁

_끝_
